- content_for :sidebar do
  %ul.selection
    - @categories.each do |cat|
      %li(cat)= link_to cat.title, category_path(page)

%ul#pages
  - @pages.each do |page|
    %li= link_to h(page.title), page_path(page)
