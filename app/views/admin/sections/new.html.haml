.grid_12
  %h2 
    = @chapter.title
    New Section

  = error_messages_for :section

  - form_for(:section, :url => admin_chapter_sections_url(@chapter), :html => { :method => :post }) do |f|
    = render :partial => "form", :locals => {:f => f}
    %p= submit_tag "Speichern"

  = link_to 'Back', admin_chapter_path(@chapter)